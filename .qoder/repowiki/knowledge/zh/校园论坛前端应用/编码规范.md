- API 模块按业务领域拆分（如 `activity.js`, `auth.js`），并统一导出基于 `http.js` 封装的请求方法
- 视图组件采用 `<script setup>` 语法糖，结合 Composition API 组织逻辑
- 状态管理使用 Pinia 的 `defineStore` 函数式写法，并在 `localStorage` 中持久化认证信息
- 路由配置采用懒加载导入组件，并通过 `meta` 字段声明权限需求（guest, auth, admin）