server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: campus-forum
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_forum?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  mail:
    host: smtp.qq.com
    port: 587
    username: 532255198@qq.com
    password: aeuijxedrwiqbghd
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

# JWT配置
jwt:
  secret: campus_forum_secret_key_2024_very_long_string_for_security
  expiration: 86400000  # 24小时（毫秒）

# AI大模型配置（DeepSeek V4 Pro）
ai:
  provider: deepseek
  api-key: sk-9617313a0c814c2fbd85a62ec356caba
  model: deepseek-v4-pro
  base-url: https://api.deepseek.com/v1/chat/completions

# 文件上传路径
upload:
  path: ./uploads/
  url-prefix: http://localhost:8080/uploads/

# Knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
